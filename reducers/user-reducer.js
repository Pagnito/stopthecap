let init = {
    user: {},
    isAuth: false
  }
  
const userReducer = (state = init, action) => {
    switch (action.type) {

  
      default:
        return state
    }
  }

  export default userReducer;